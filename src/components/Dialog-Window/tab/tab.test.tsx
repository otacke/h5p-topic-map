import * as React from "react"


export type DialogTabProps = {
    titles: string[];
}

export const DialogTab: React.FC<DialogTabProps> = ({
    titles,
}): JSX.Element => {
    return(
        <div> </div>
    )
}