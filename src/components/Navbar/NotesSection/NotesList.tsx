import * as React from "react";
import styles from "./NotesList.module.scss";

export const NotesList: React.FC = () => {
  return (
    <div className={styles.mainBody}>
      <div className={styles.mainBodyContent}>Test</div>
    </div>
  );
};
